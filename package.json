{
  "name": "metabase-agent-api-demo",
  "description": "A sample chat app for the Metabase Agent API",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/metabase/metabase-agent-api-demo.git"
  },
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "npx concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client"
  }
}
